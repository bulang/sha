const jwt = require("jsonwebtoken");
const ROOT_PATH = process.cwd();
const baseCfg = require(`${ROOT_PATH}/config/base`);
/**
 * Token class
 */
class Token {
    constructor(){

    }

    createToken(data,exp){
        
    }

    checkoutToken(){

    }
}