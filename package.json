{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://gitee.com/bulang/sha.git"
  },
  "author": "{{author}}",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "jsonwebtoken": "^8.5.1",
    "koa": "^2.7.0",
    "koa-router": "^7.4.0"
  }
}
